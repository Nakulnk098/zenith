services:
  - type: web
    name: zenith
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: zenith-data
      mountPath: /opt/render/project/src
      sizeGB: 1
